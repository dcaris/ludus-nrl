class Club {
    /**
     * Creates an instance of Club.
     * @param {string} nickname 
     * @param {string} fullName 
     * @memberof Club
     */
    constructor(nickname, fullName) {
        this.nickname = nickname;
        this.fullName = fullName;
    }
}

module.exports = Club;